{
  "name": "cs-sharecode-cli",
  "version": "1.0.0",
  "description": "Standalone CLI tool to get CS demo download links from share codes",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "postbuild": "node -e \"if (process.platform === 'linux') { const fs = require('fs-extra'); const path = require('path'); const src = path.join(__dirname, 'bin', 'linux-x64', 'boiler-writter'); const dest = path.join(__dirname, 'dist', 'boiler-writter'); if (fs.existsSync(src)) { fs.copySync(src, dest); fs.chmodSync(dest, 0o755); console.log('Copied local Linux boiler-writter executable to dist.'); } else { console.error('ERROR: Local boiler-writter linux executable not found at:', src); process.exit(1); } }\"",
    "dev": "node --loader ts-node/esm index.ts",
    "start:api": "node dist/api.js"
  },
  "dependencies": {
    "@akiver/boiler-writter": "^1.6.0",
    "commander": "^12.0.0",
    "csgo-protobuf": "^3.0.16",
    "csgo-sharecode": "^3.1.2",
    "fs-extra": "^11.3.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.17.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "bin": {
    "cs-sharecode": "./index.js"
  },
  "keywords": [
    "counter-strike",
    "cs2",
    "csgo",
    "demo",
    "sharecode",
    "cli"
  ],
  "author": "Generated from CS Demo Manager",
  "license": "MIT"
}
